pre-commit:
  parallel: true
  commands:
    format-check:
      run: pnpm format:check
    lint:
      run: pnpm lint

commit-msg:
  commands:
    commitlint:
      run: npx --no -- commitlint --edit "$LEFTHOOK_COMMIT_MSG"
