---
phase: 02-build-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/tsup.config.ts
  - packages/eslint-config/tsup.config.ts
autonomous: true
requirements: [BLD-01, BLD-02, BLD-03, BLD-04]

must_haves:
  truths:
    - "turbo typecheck exits 0 with zero TS5101 warnings in stdout/stderr"
    - "turbo build exits 0 with zero TS5101 warnings in stdout/stderr"
    - "DTS .d.ts files exist in packages/ui/dist/ and packages/eslint-config/dist/ after build"
    - "turbo test exits 0 with all tests passing and no new failures"
    - "tsup.config.ts files contain comments explaining ignoreDeprecations workaround and linking tsup#1388"
  artifacts:
    - path: "packages/ui/tsup.config.ts"
      provides: "Documented tsup DTS workaround for UI package"
      contains: "tsup#1388"
    - path: "packages/eslint-config/tsup.config.ts"
      provides: "Documented tsup DTS workaround for eslint-config package"
      contains: "tsup#1388"
    - path: "packages/ui/dist"
      provides: "Built UI package output including DTS files"
    - path: "packages/eslint-config/dist"
      provides: "Built eslint-config output including DTS files"
  key_links:
    - from: "turbo typecheck"
      to: "tsc --noEmit in each package"
      via: "turbo task graph"
      pattern: "typecheck.*tsc --noEmit"
    - from: "turbo build"
      to: "tsup in ui and eslint-config, vite in showcase"
      via: "turbo task graph with ^build dependency"
      pattern: "build.*tsup|vite build"
    - from: "tsup dts.compilerOptions"
      to: "ignoreDeprecations: 6.0"
      via: "scoped workaround for tsup#1388"
      pattern: "ignoreDeprecations.*6\\.0"
---

<objective>
Verify the full monorepo builds, typechecks, and tests pass cleanly under TypeScript 6 with zero errors and zero deprecation warnings. Add documentation comments to tsup workaround configs and fix any breakage found.

Purpose: This is the final validation that the TS6 migration (Phase 1) is complete and correct. The template must be a clean slate for new projects.
Output: Documented tsup configs, verified clean build/typecheck/test runs, any fixes applied.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/Documents/base-monorepo-template/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/Documents/base-monorepo-template/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-catalog-bump-tsconfig-cleanup/01-01-SUMMARY.md

<interfaces>
<!-- Key config structures the executor needs. Extracted from codebase. -->

From packages/ui/tsup.config.ts:
```typescript
import { glob } from "glob"
import { defineConfig } from "tsup"

export default defineConfig([
  {
    entry: [...glob.sync("src/components/*.tsx"), ...glob.sync("src/hooks/*.ts")],
    format: ["esm"],
    dts: { compilerOptions: { ignoreDeprecations: "6.0" } },
    sourcemap: true,
    clean: true,
    external: ["react", "react-dom"],
    banner: { js: '"use client";' },
    outDir: "dist",
    outExtension: () => ({ js: ".js" }),
  },
  {
    entry: ["src/index.ts", "src/lib/utils.ts"],
    format: ["esm"],
    dts: { compilerOptions: { ignoreDeprecations: "6.0" } },
    sourcemap: true,
    clean: false,
    external: ["react", "react-dom"],
    outDir: "dist",
    outExtension: () => ({ js: ".js" }),
  },
])
```

From packages/eslint-config/tsup.config.ts:
```typescript
import { defineConfig } from "tsup"

export default defineConfig({
  entry: ["src/index.ts"],
  format: ["esm"],
  dts: { compilerOptions: { ignoreDeprecations: "6.0" } },
  splitting: false,
  sourcemap: true,
  clean: true,
  treeshake: true,
  minify: false,
  target: "node24",
  outDir: "dist",
})
```

From turbo.json (task graph):
```json
{
  "tasks": {
    "typecheck": { "dependsOn": ["^build"] },
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] },
    "test": { "dependsOn": ["^build"], "outputs": ["coverage/**"] }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document tsup ignoreDeprecations workaround in both config files</name>
  <files>packages/ui/tsup.config.ts, packages/eslint-config/tsup.config.ts</files>
  <read_first>
    - packages/ui/tsup.config.ts
    - packages/eslint-config/tsup.config.ts
  </read_first>
  <action>
Add a code comment above each `dts: { compilerOptions: { ignoreDeprecations: "6.0" } }` line in both tsup config files, per D-03. The comment must:
1. Explain WHY the workaround exists (tsup's DTS builder internally uses the deprecated `baseUrl` option)
2. Link to the upstream issue (https://github.com/egoist/tsup/issues/1388)
3. Make the workaround grep-able for future cleanup

In `packages/ui/tsup.config.ts`, add the comment above BOTH `dts:` lines (lines 9 and 21). The comment for each should be:
```
// Workaround: tsup DTS builder uses deprecated baseUrl internally (tsup#1388)
// https://github.com/egoist/tsup/issues/1388 — remove when tsup fixes this
```

In `packages/eslint-config/tsup.config.ts`, add the same comment above the single `dts:` line (line 6):
```
// Workaround: tsup DTS builder uses deprecated baseUrl internally (tsup#1388)
// https://github.com/egoist/tsup/issues/1388 — remove when tsup fixes this
```

Do NOT change any other lines. Do NOT modify the `ignoreDeprecations` value or any other config option.
  </action>
  <verify>
    <automated>grep -c "tsup#1388" packages/ui/tsup.config.ts && grep -c "tsup#1388" packages/eslint-config/tsup.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/ui/tsup.config.ts contains the string "tsup#1388" exactly 2 times (once above each dts line)
    - packages/eslint-config/tsup.config.ts contains the string "tsup#1388" exactly 1 time
    - packages/ui/tsup.config.ts contains the string "remove when tsup fixes this" exactly 2 times
    - packages/eslint-config/tsup.config.ts contains the string "remove when tsup fixes this" exactly 1 time
    - Both files still contain `ignoreDeprecations: "6.0"` unchanged
    - No other lines modified
  </acceptance_criteria>
  <done>Both tsup config files have workaround comments with tsup#1388 link above every ignoreDeprecations line, making the workaround grep-able for future cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: Run full typecheck and build verification with TS5101 grep</name>
  <files></files>
  <read_first>
    - turbo.json
    - packages/ui/tsup.config.ts
    - packages/eslint-config/tsup.config.ts
  </read_first>
  <action>
Run the full monorepo typecheck and build pipeline using turbo orchestration (per D-04), then verify results (per D-05, D-06).

**Step 1 — Typecheck:**
Run `pnpm turbo typecheck --force` (force to bypass cache and get fresh output). Capture both stdout and stderr.

**Step 2 — Grep for TS5101 in typecheck output:**
Per D-06, explicitly grep the typecheck output for "TS5101" or "deprecated" or "deprecation". A zero exit code with deprecation warnings in the output is still a failure. The grep must return zero matches.

**Step 3 — Build:**
Run `pnpm turbo build --force`. Capture both stdout and stderr.

**Step 4 — Grep for TS5101 in build output:**
Same as Step 2 — grep build output for "TS5101", "deprecated", "deprecation" warnings. Zero matches required.

**Step 5 — Verify DTS output:**
Check that `.d.ts` files exist in:
- `packages/ui/dist/` (should have component .d.ts files like `button.d.ts`)
- `packages/eslint-config/dist/` (should have `index.d.ts`)

Run: `ls packages/ui/dist/*.d.ts | head -5` and `ls packages/eslint-config/dist/*.d.ts`

**If any step fails (per D-01, D-02):**
- Investigate root cause before fixing (D-02 — do not blindly update assertions)
- Fix everything found, TS6-related or not (D-01 — clean slate)
- Re-run the failed step to confirm the fix
- Document what was fixed and why in the SUMMARY

**Pass criteria:** Both turbo typecheck and turbo build exit 0, zero TS5101/deprecation matches in output, DTS files present in both dist/ directories.
  </action>
  <verify>
    <automated>pnpm turbo typecheck --force 2>&1 | grep -ci "TS5101\|deprecat" | grep -q "^0$" && pnpm turbo build --force 2>&1 | grep -ci "TS5101\|deprecat" | grep -q "^0$" && ls packages/ui/dist/*.d.ts >/dev/null 2>&1 && ls packages/eslint-config/dist/index.d.ts >/dev/null 2>&1 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm turbo typecheck --force` exits with code 0
    - Typecheck output contains zero lines matching "TS5101" (case-insensitive grep returns 0)
    - `pnpm turbo build --force` exits with code 0
    - Build output contains zero lines matching "TS5101" (case-insensitive grep returns 0)
    - `packages/ui/dist/` contains at least one `.d.ts` file
    - `packages/eslint-config/dist/index.d.ts` exists
    - If any failures were found and fixed, the fix is documented
  </acceptance_criteria>
  <done>turbo typecheck and turbo build both exit 0 with zero TS5101 deprecation warnings in output. DTS files are generated in both package dist/ directories. BLD-01, BLD-02, BLD-03 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite verification</name>
  <files></files>
  <read_first>
    - turbo.json
  </read_first>
  <action>
Run the full monorepo test suite using turbo orchestration (per D-04).

**Step 1 — Run tests:**
Run `pnpm turbo test --force` to execute all test suites (Vitest in ui and showcase packages). Capture both stdout and stderr.

**Step 2 — Check results:**
Verify that all test suites pass. The output should show zero failed tests.

**If any tests fail (per D-01, D-02):**
- Investigate root cause before fixing (D-02 — understand WHY the test fails, not just what assertion broke)
- Determine if the failure is TS6-related (e.g., type behavior change) or pre-existing
- Fix the root cause, not the symptom (do not blindly update `.toEqual()` values)
- Re-run `pnpm turbo test --force` to confirm the fix
- Document what was fixed and why in the SUMMARY

**Pass criteria:** `turbo test` exits 0 with all tests passing.
  </action>
  <verify>
    <automated>pnpm turbo test --force 2>&1 | tail -20 && echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm turbo test --force` exits with code 0
    - Output shows "Tests" with all passing (zero failed test files)
    - If any failures were found and fixed, the fix is documented with root cause analysis
  </acceptance_criteria>
  <done>turbo test exits 0 with all existing tests passing. No new failures introduced by TS6 migration. BLD-04 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local build tooling | All commands run locally — no network services, no user input, no deployments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | pnpm-lock.yaml | accept | Lockfile already committed from Phase 1. No `pnpm install` in this phase — only running existing build/test commands. No dependency resolution changes. |
| T-02-02 | Information Disclosure | Build output | accept | Build runs locally, no CI/CD secrets involved. This is a template repo with no .env files or credentials. |

Phase 2 is pure local verification (typecheck, build, test). No network calls, no authentication, no user-facing surfaces, no data storage. Threat surface is minimal.
</threat_model>

<verification>
Run all three verification checks in sequence:

1. `grep -c "tsup#1388" packages/ui/tsup.config.ts` returns `2`
2. `grep -c "tsup#1388" packages/eslint-config/tsup.config.ts` returns `1`
3. `pnpm turbo typecheck --force` exits 0, output has zero TS5101 matches
4. `pnpm turbo build --force` exits 0, output has zero TS5101 matches
5. `ls packages/ui/dist/*.d.ts` succeeds (DTS files exist)
6. `ls packages/eslint-config/dist/index.d.ts` succeeds
7. `pnpm turbo test --force` exits 0, all tests pass
</verification>

<success_criteria>
- All four BLD requirements (BLD-01 through BLD-04) are satisfied
- Zero TS5101 deprecation warnings in any turbo output
- DTS files generated correctly with scoped workaround
- Workaround is documented with tsup#1388 reference in both config files
- All existing tests pass with no new failures
- Template is ready to serve as a clean TS6 starting point
</success_criteria>

<output>
After completion, create `.planning/phases/02-build-verification/02-01-SUMMARY.md`
</output>
