---
phase: 01-catalog-bump-tsconfig-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - pnpm-lock.yaml
  - packages/eslint-config/package.json
  - apps/showcase/tsconfig.json
  - packages/ui/tsconfig.json
  - packages/eslint-config/tsconfig.json
  - packages/tsconfig/react-app.json
  - packages/tsconfig/react-library.json
autonomous: true
requirements: [CAT-01, CAT-02, TSC-01, TSC-02, TSC-03, TSC-04, TSC-05]

must_haves:
  truths:
    - "TypeScript 6.0.2+ resolves for all packages in the monorepo"
    - "No TS5101 deprecation warnings from tsc --noEmit on any package"
    - "Path aliases (@/*, @ui, @ui/*) still resolve correctly in all packages"
    - "React types and vitest globals are recognized by the compiler in react presets"
    - "eslint-config accepts TypeScript v6 as a peer dependency"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "TypeScript catalog version ^6.0.2"
      contains: "typescript: ^6.0.2"
    - path: "packages/eslint-config/package.json"
      provides: "Forward-compatible TypeScript peer dependency"
      contains: ">=5.0.0"
    - path: "apps/showcase/tsconfig.json"
      provides: "Showcase tsconfig without deprecated baseUrl"
    - path: "packages/ui/tsconfig.json"
      provides: "UI tsconfig without deprecated baseUrl"
    - path: "packages/eslint-config/tsconfig.json"
      provides: "ESLint-config tsconfig without deprecated baseUrl"
    - path: "packages/tsconfig/react-app.json"
      provides: "React app preset with explicit types array"
      contains: "vitest/globals"
    - path: "packages/tsconfig/react-library.json"
      provides: "React library preset with explicit types array"
      contains: "vitest/globals"
  key_links:
    - from: "pnpm-workspace.yaml"
      to: "all packages"
      via: "pnpm catalog protocol"
      pattern: "typescript: \\^6"
    - from: "packages/tsconfig/react-app.json"
      to: "apps/showcase/tsconfig.json"
      via: "extends field"
      pattern: "extends.*react-app"
    - from: "packages/tsconfig/react-library.json"
      to: "packages/ui/tsconfig.json"
      via: "extends field"
      pattern: "extends.*react-library"
---

<objective>
Upgrade TypeScript from 5.9.3 to 6.0.2 in the pnpm catalog and remove all deprecated tsconfig options so the monorepo compiles without any TS5101 warnings.

Purpose: This is the core migration step -- after this plan, the template runs on TS6 with a clean config. The catalog bump, baseUrl removal, types addition, and peer dep update must land atomically (splitting leaves CI broken).

Output: All 8 config files updated, pnpm lockfile regenerated, tsc --noEmit passes on every package with zero warnings.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/Documents/base-monorepo-template/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/Documents/base-monorepo-template/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-catalog-bump-tsconfig-cleanup/01-CONTEXT.md
@.planning/phases/01-catalog-bump-tsconfig-cleanup/01-RESEARCH.md

<interfaces>
<!-- Current state of files being modified. Executor MUST read these via read_first. -->

From pnpm-workspace.yaml (line 22):
```yaml
  typescript: ^5.9.3
```

From packages/eslint-config/package.json (line 32):
```json
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
```

From packages/tsconfig/react-app.json:
```json
{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./base.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "noEmit": true,
    "allowImportingTsExtensions": true
  }
}
```

From packages/tsconfig/react-library.json:
```json
{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./base.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  }
}
```

From apps/showcase/tsconfig.json:
```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": { "@/*": ["./src/*"] }
  }
}
```

From packages/ui/tsconfig.json:
```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": { "@ui": ["./src/index.ts"], "@ui/*": ["./src/*"] }
  }
}
```

From packages/eslint-config/tsconfig.json:
```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": { "@/*": ["./src/*"] }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump TypeScript catalog and update peer dependency</name>
  <files>pnpm-workspace.yaml, pnpm-lock.yaml, packages/eslint-config/package.json</files>
  <read_first>
    - pnpm-workspace.yaml (source of truth for catalog versions -- see line 22 for current typescript entry)
    - packages/eslint-config/package.json (peer dependency on line 32 -- currently "^5.0.0")
  </read_first>
  <action>
1. Edit `pnpm-workspace.yaml`: Change the catalog entry `typescript: ^5.9.3` to `typescript: ^6.0.2` (per CAT-01). This is line 22 in the file. Do NOT change any other catalog entry.

2. Edit `packages/eslint-config/package.json`: In the `peerDependencies` object, change `"typescript": "^5.0.0"` to `"typescript": ">=5.0.0"` (per D-04 / CAT-02). This is the only change in this file.

3. Run `pnpm install` from the repo root. This will:
   - Resolve TypeScript 6.0.2 from the registry
   - Update `pnpm-lock.yaml` with the new resolution
   - Note: `preferFrozenLockfile: true` does NOT block developer installs, only CI. The lockfile update is expected and correct.

4. Verify the installed version: Run `pnpm -r exec -- pnpm list typescript --depth=0` and confirm all packages resolve to TypeScript 6.x.
  </action>
  <verify>
    <automated>cd /Users/pauvelascogarrofe/Documents/base-monorepo-template && pnpm list typescript --depth=0 -r 2>&1 | grep -E "typescript [67]\."</automated>
  </verify>
  <acceptance_criteria>
    - pnpm-workspace.yaml contains the string `typescript: ^6.0.2` (not `^5.9.3`)
    - packages/eslint-config/package.json contains `"typescript": ">=5.0.0"` in peerDependencies (not `"^5.0.0"`)
    - pnpm-lock.yaml has been regenerated (git diff shows changes)
    - `pnpm list typescript -r` shows TypeScript 6.x for all packages
  </acceptance_criteria>
  <done>TypeScript 6.0.2+ resolves for all workspace packages. eslint-config peer dependency accepts v6.</done>
</task>

<task type="auto">
  <name>Task 2: Remove baseUrl from consumer tsconfigs and add types to presets</name>
  <files>apps/showcase/tsconfig.json, packages/ui/tsconfig.json, packages/eslint-config/tsconfig.json, packages/tsconfig/react-app.json, packages/tsconfig/react-library.json</files>
  <read_first>
    - apps/showcase/tsconfig.json (has baseUrl on line 6 -- must be removed)
    - packages/ui/tsconfig.json (has baseUrl on line 6 -- must be removed)
    - packages/eslint-config/tsconfig.json (has baseUrl on line 6 -- must be removed)
    - packages/tsconfig/react-app.json (needs types array added to compilerOptions)
    - packages/tsconfig/react-library.json (needs types array added to compilerOptions)
    - packages/tsconfig/base.json (read-only reference -- confirm it has NO types field and NO baseUrl, per D-01)
  </read_first>
  <action>
**baseUrl removal (TSC-01, TSC-02, TSC-03):**

1. Edit `apps/showcase/tsconfig.json`: Remove the `"baseUrl": "."` line (line 6). Keep everything else unchanged. The `paths` entry `"@/*": ["./src/*"]` already uses a `./`-prefixed path and resolves without baseUrl in TS6. Final compilerOptions should contain only `paths`.

   Target state:
   ```json
   {
     "extends": "@monorepo-template/tsconfig/react-app.json",
     "include": ["src", "*.ts"],
     "exclude": ["node_modules"],
     "compilerOptions": {
       "paths": {
         "@/*": ["./src/*"]
       }
     }
   }
   ```

2. Edit `packages/ui/tsconfig.json`: Remove the `"baseUrl": "."` line (line 6). Keep everything else unchanged. The paths entries `"@ui": ["./src/index.ts"]` and `"@ui/*": ["./src/*"]` already use `./`-prefixed paths.

   Target state:
   ```json
   {
     "extends": "@monorepo-template/tsconfig/react-library.json",
     "include": ["src", "*.ts"],
     "exclude": ["node_modules", "dist"],
     "compilerOptions": {
       "paths": {
         "@ui": ["./src/index.ts"],
         "@ui/*": ["./src/*"]
       }
     }
   }
   ```

3. Edit `packages/eslint-config/tsconfig.json`: Remove the `"baseUrl": "."` line (line 6). Keep everything else unchanged.

   Target state:
   ```json
   {
     "extends": "@monorepo-template/tsconfig/base.json",
     "include": ["src", "*.ts"],
     "exclude": ["node_modules", "dist"],
     "compilerOptions": {
       "paths": {
         "@/*": ["./src/*"]
       }
     }
   }
   ```

**types field addition (TSC-04, per D-01 and D-02):**

4. Edit `packages/tsconfig/react-app.json`: Add `"types": ["react", "react-dom", "vitest/globals"]` to `compilerOptions`. This is needed because TS6 changes the `types` default to `[]`, which stops auto-inclusion of `@types/*` packages. The react-app preset is used by `apps/showcase` which needs React types and vitest globals (vitest.config has `globals: true`).

   Target state:
   ```json
   {
     "$schema": "https://json.schemastore.org/tsconfig",
     "extends": "./base.json",
     "compilerOptions": {
       "jsx": "react-jsx",
       "noEmit": true,
       "allowImportingTsExtensions": true,
       "types": ["react", "react-dom", "vitest/globals"]
     }
   }
   ```

5. Edit `packages/tsconfig/react-library.json`: Add `"types": ["react", "react-dom", "vitest/globals"]` to `compilerOptions`. Same rationale -- the UI package runs vitest tests with `globals: true` and needs React types.

   Target state:
   ```json
   {
     "$schema": "https://json.schemastore.org/tsconfig",
     "extends": "./base.json",
     "compilerOptions": {
       "jsx": "react-jsx",
       "declaration": true,
       "declarationMap": true,
       "sourceMap": true,
       "types": ["react", "react-dom", "vitest/globals"]
     }
   }
   ```

6. Do NOT add a `types` field to `packages/tsconfig/base.json` (per D-01 -- base stays minimal; eslint-config does not use @types packages).

7. Do NOT modify `packages/tsconfig/base.json` at all -- `preserveConstEnums: true` is NOT deprecated in TS6.
  </action>
  <verify>
    <automated>cd /Users/pauvelascogarrofe/Documents/base-monorepo-template && grep -rn "baseUrl" apps/showcase/tsconfig.json packages/ui/tsconfig.json packages/eslint-config/tsconfig.json; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - apps/showcase/tsconfig.json does NOT contain the string "baseUrl"
    - packages/ui/tsconfig.json does NOT contain the string "baseUrl"
    - packages/eslint-config/tsconfig.json does NOT contain the string "baseUrl"
    - apps/showcase/tsconfig.json contains `"@/*": ["./src/*"]` (paths preserved)
    - packages/ui/tsconfig.json contains `"@ui": ["./src/index.ts"]` (paths preserved)
    - packages/tsconfig/react-app.json contains `"types": ["react", "react-dom", "vitest/globals"]`
    - packages/tsconfig/react-library.json contains `"types": ["react", "react-dom", "vitest/globals"]`
    - packages/tsconfig/base.json does NOT contain a "types" field (unchanged)
  </acceptance_criteria>
  <done>All 3 consumer tsconfigs have baseUrl removed with paths intact. Both react presets declare explicit types arrays. base.json is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Verify zero TS5101 warnings and path alias resolution</name>
  <files></files>
  <read_first>
    - packages/tsconfig/base.json (reference -- confirm extends @tsconfig/node24 is still present per D-03)
    - .planning/phases/01-catalog-bump-tsconfig-cleanup/01-RESEARCH.md (see TSC-05 verification and Assumptions A1-A3)
  </read_first>
  <action>
Run `tsc --noEmit` on each package to verify TSC-05 (no TS5101 warnings from @tsconfig/node24 or any other source) and confirm the entire config is clean (per D-05).

1. Run `cd /Users/pauvelascogarrofe/Documents/base-monorepo-template/packages/eslint-config && pnpm tsc --noEmit 2>&1`. Expect zero errors and zero TS5101 warnings.

2. Run `cd /Users/pauvelascogarrofe/Documents/base-monorepo-template/packages/ui && pnpm tsc --noEmit 2>&1`. Expect zero errors and zero TS5101 warnings. This also validates that:
   - `@ui` and `@ui/*` path aliases still resolve (TSC-02)
   - React types are recognized via the types array (TSC-04)
   - vitest globals (describe, it, expect) are recognized (TSC-04)
   - @testing-library/jest-dom matchers are recognized via setup file import (Assumption A1)

3. Run `cd /Users/pauvelascogarrofe/Documents/base-monorepo-template/apps/showcase && pnpm tsc --noEmit 2>&1`. Expect zero errors and zero TS5101 warnings. This validates:
   - `@/*` path alias still resolves (TSC-01)
   - React types are recognized (TSC-04)

4. If any TS5101 warnings appear, they indicate a deprecated option is still present. Read the warning to identify which option and which tsconfig file, then fix it.

5. If type errors appear (not TS5101 warnings), investigate:
   - Missing types in the `types` array: add the needed entry
   - Path alias resolution failure: check that paths entries use `./` prefix
   - @testing-library/jest-dom matchers not recognized: add `"@testing-library/jest-dom"` to the types array (fallback for Assumption A1)
   - vitest globals not recognized: confirm the types entry is `"vitest/globals"` not `"vitest"` (Assumption A3)

6. Alternatively, run `pnpm turbo typecheck` if the turbo task is configured, which runs tsc --noEmit on all packages.
  </action>
  <verify>
    <automated>cd /Users/pauvelascogarrofe/Documents/base-monorepo-template && pnpm turbo typecheck 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits with code 0 in packages/eslint-config
    - `pnpm tsc --noEmit` exits with code 0 in packages/ui
    - `pnpm tsc --noEmit` exits with code 0 in apps/showcase
    - Output of all three commands contains zero lines matching "TS5101" (no deprecation warnings)
    - Output of all three commands contains zero lines matching "error TS" (no type errors)
  </acceptance_criteria>
  <done>All packages typecheck cleanly under TypeScript 6. Zero TS5101 warnings. Zero type errors. @tsconfig/node24 preset confirmed compatible (TSC-05). Path aliases resolve correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local install | TypeScript 6.0.2 package fetched from npm during pnpm install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | pnpm-workspace.yaml catalog | accept | Low risk -- catalog is a version constraint, not executable code. pnpm verifies package integrity via lockfile hashes. |
| T-01-02 | Tampering | npm registry package | accept | pnpm lockfile records integrity hashes. `preferFrozenLockfile: true` in CI prevents unexpected resolution changes. Standard npm supply chain trust model. |
| T-01-03 | Denial of Service | types array misconfiguration | accept | Incorrect types array causes compile errors, not security issues. Caught immediately by tsc --noEmit verification in Task 3. |
</threat_model>

<verification>
After all three tasks complete:

1. `grep "typescript: \\^6" pnpm-workspace.yaml` returns a match
2. `grep ">=5.0.0" packages/eslint-config/package.json` returns a match
3. `grep -r "baseUrl" apps/showcase/tsconfig.json packages/ui/tsconfig.json packages/eslint-config/tsconfig.json` returns NO matches
4. `grep "vitest/globals" packages/tsconfig/react-app.json packages/tsconfig/react-library.json` returns matches in both files
5. `pnpm turbo typecheck` exits 0 with no TS5101 warnings
</verification>

<success_criteria>
- TypeScript 6.0.2+ installed and resolving for all workspace packages
- Zero TS5101 deprecation warnings from tsc --noEmit on any package
- Path aliases (@/*, @ui, @ui/*) still resolve in all consuming packages
- React types and vitest globals recognized by the compiler in react presets
- eslint-config peer dependency accepts TypeScript v5 and v6
- base.json unchanged (no types field, no modifications)
- @tsconfig/node24 preset produces no warnings under TS6
</success_criteria>

<output>
After completion, create `.planning/phases/01-catalog-bump-tsconfig-cleanup/01-01-SUMMARY.md`
</output>
